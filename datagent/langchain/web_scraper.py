def scrape_url(url):
    """
    Scrape the content of a given URL using Langchain's Scraper.
    Math notation $e = mc^2$ is supported.

    $$
    \sum_{i=1}^n x_i = w_1
    $$

    Parameters:
        url (str): The URL to scrape.

    Returns:
        str: The scraped content.
    """
    # Function implementation...
